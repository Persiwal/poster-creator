const ResizeHandler = () => {
  return (
    <button className="absolute right-[-12px] bottom-[-12px] w-6 h-6 border-[4px] border-white bg-primary rounded-full"></button>
  );
};

export default ResizeHandler;
